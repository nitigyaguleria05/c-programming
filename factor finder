#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int N;
    if (scanf("%d", &N) != 1) return 0;

    if (N == 0) {
        printf("0\n");
        return 0;
    }

    if (N < 0) N = -N;

    for (int i = 1; i <= N; i++) {
        if (N % i == 0) {
            printf("%d", i);
            if (i != N) printf(" ");
        }
    }
    printf("\n");

    return 0;
}
