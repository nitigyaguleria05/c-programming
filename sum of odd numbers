#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    long long N;
    if (scanf("%lld", &N) != 1) return 0;
    long long k = (N + 1) / 2;
    long long sum = k * k; // sum of first k odd numbers = k^2
    printf("%lld\n", sum);
    return 0;
}
