#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    long long N;
    if (scanf("%lld", &N) != 1) return 0;
    for (int i = 1; i <= 10; ++i) {
        printf("%lld x %d = %lld\n", N, i, N * i);
    }
    return 0;
}
