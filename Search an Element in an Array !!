#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int N, key, found = 0;
    scanf("%d", &N);  

    int arr[N];  

    
    for (int i = 0; i < N; i++) {
        scanf("%d", &arr[i]);
    }

    
    scanf("%d", &key);


    for (int i = 0; i < N; i++) {
        if (arr[i] == key) {
            printf("Element found at position %d\n", i + 1);
            found = 1;
            break;
        }
    }

    if (!found) {
        printf("Element not found\n");
    }

    return 0;
}
