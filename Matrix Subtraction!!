#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int R, C;
    scanf("%d %d", &R, &C);

    int A[R][C], B[R][C], diff[R][C];

    
    for (int i = 0; i < R; i++)
        for (int j = 0; j < C; j++)
            scanf("%d", &A[i][j]);

    
    for (int i = 0; i < R; i++)
        for (int j = 0; j < C; j++)
            scanf("%d", &B[i][j]);

    
    for (int i = 0; i < R; i++) {
        for (int j = 0; j < C; j++) {
            diff[i][j] = A[i][j] - B[i][j];
            printf("%d ", diff[i][j]);
        }
        printf("\n");
    }

    return 0;
}
