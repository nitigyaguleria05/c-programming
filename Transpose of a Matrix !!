#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int R, C;
    scanf("%d %d", &R, &C);

    int A[R][C], T[C][R];

    
    for (int i = 0; i < R; i++)
        for (int j = 0; j < C; j++)
            scanf("%d", &A[i][j]);

    
    for (int i = 0; i < C; i++) {
        for (int j = 0; j < R; j++) {
            T[i][j] = A[j][i];
            printf("%d ", T[i][j]);
        }
        printf("\n");
    }

    return 0;
}
